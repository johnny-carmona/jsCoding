{
  "name": "mdljstest",
  "version": "1.0.0",
  "description": "coding test, decided to use node and babel to transpile the code",
  "main": "index.js",
  "scripts": {
    "start": "",
    "build": "babel --presets es2015 src -w -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Johnny Carmona",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
